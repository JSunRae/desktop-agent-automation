# Optional constraints for dev tooling versions.
#
# Keep this file small and focused (dev tools only) so it stays portable.
# Update as needed via:
#   python -m pip install -e ".[dev]" --upgrade
#   python -m pip freeze | Select-String -Pattern '^(pytest|pytest-cov|black|pylint)=='
#
black==25.12.0
pylint==4.0.4
pytest==9.0.1
pytest-cov==7.0.0
coverage==7.13.0
isort==7.0.0
astroid==4.0.2
